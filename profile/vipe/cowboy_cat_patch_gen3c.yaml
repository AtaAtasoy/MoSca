patch_name: cowboy_cat_gen3c_patch

# Existing MoSca run to patch
base_logdir: /home/atasoy/MoSca/runs/cowboy_cat_original_scale/logs/cowboy_cat_vipe_fixed_scene_native_add3_20260401_153850
base_ws: /home/atasoy/MoSca/runs/cowboy_cat_original_scale
base_fit_cfg: /home/atasoy/MoSca/profile/vipe/cowboy_cat_fit_fixed_scene.yaml

# Fixed gen3c supervision sequence
patch_sequence_name: cowboy_cat_gen3c
gen3c_rgb_dir: /home/atasoy/MoSca/gen3c-prior/cowboy-cat-zoom-in-out/gen3c/output/rgb/frames
gen3c_pose_path: /home/atasoy/MoSca/gen3c-prior/cowboy-cat-zoom-in-out/gen3c/output/pose/normalized_nofilter.npz
gen3c_intrinsics_path: /home/atasoy/MoSca/gen3c-prior/cowboy-cat-zoom-in-out/gen3c/output/intrinsics/normalized_nofilter.npz
gen3c_normalization_params: /home/atasoy/vipe/vipe_results/121frames/cowboy-cat/scene/normalized_nofilter/normalization_params.json
gen3c_normalization_mode: normalized_to_raw

# Optional future prior workspace for depth/flow/TAP hooks.
# The current implementation keeps the gen3c branch RGB-only even if this is set.
gen3c_prior_ws: null

patch_phase_name: patch_gen3c
patch_save_preview_renders: true

patch_anchor_weight: 0.5
patch_gen3c_rgb_weight: 1.0
patch_gen3c_rgb_ssim_lambda: 0.1

patch_anchor_views_per_step: 1
patch_gen3c_views_per_step: 1

# Stage 1: Appearance-only updates
patch_stage1_steps: 2000
# Step2: Conservative geometry updates at 0.25x
patch_stage2_steps: 2000
# Step3: Full geometry LR plus densify/prune/reset controls.
patch_stage3_steps: 4000

patch_stage2_geo_lr_scale: 0.25
patch_stage2_node_lr_scale: 0.25
patch_stage2_node_sigma_lr_scale: 0.25
patch_stage3_geo_lr_scale: 1.0
patch_stage3_node_lr_scale: 1.0
patch_stage3_node_sigma_lr_scale: 1.0

patch_use_decay: false
patch_decay_start: 2000
patch_geo_reg_start_steps: 0
patch_skinning_corr_start_steps: 10000000000

patch_s_ctrl_densify_steps: 400
patch_s_ctrl_prune_steps: 400
patch_s_ctrl_reset_steps: 1000
patch_s_ctrl_densify_max_grad: 0.0002
patch_s_ctrl_prune_opacity_th: 0.02
patch_s_ctrl_reset_opacity: 0.01

patch_d_ctrl_densify_steps: 300
patch_d_ctrl_prune_steps: 300
patch_d_ctrl_reset_steps: 1000
patch_d_ctrl_densify_max_grad: 0.0001
patch_d_ctrl_prune_opacity_th: 0.02
patch_d_ctrl_reset_opacity: 0.01

patch_viz_cheap_interval: 500
patch_viz_skip_t: 1
patch_viz_move_angle_deg: 10.0
